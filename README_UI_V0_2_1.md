# WoRP Lab UI V0.2.1

Patch over UI V0.2:
- Adds case-insensitive league search/filter.
- Shows how many leagues match.
- Keeps the validated Sleeper USER → LEAGUES → FORMAT → ROSTERS path unchanged.
- Does not modify `worp_engine.py`.

Run:
```bash
python3.12 -m streamlit run app_v0_2_1.py
```
